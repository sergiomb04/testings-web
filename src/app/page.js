import DarkModeButton from "@/app/components/DarkModeButton";

export default function Home() {
    return (
        <main>
            <h1>Test</h1>
            <DarkModeButton />
        </main>
    );
}
